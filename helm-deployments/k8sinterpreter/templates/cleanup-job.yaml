{{- if .Values.execution.cleanup.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "k8sinterpreter.fullname" . }}-cleanup
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8sinterpreter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  ttlSecondsAfterFinished: 60
  template:
    spec:
      serviceAccountName: {{ include "k8sinterpreter.serviceAccountName" . }}
      restartPolicy: Never
      containers:
        - name: cleanup
          image: bitnami/kubectl:latest
          command:
            - /bin/sh
            - -c
            - |
              echo "Cleaning up pool pods..."
              kubectl delete pods -n {{ .Release.Namespace }} \
                -l app.kubernetes.io/managed-by=k8sinterpreter \
                -l k8sinterpreter.io/type=pool \
                --ignore-not-found=true
              echo "Cleaning up any remaining jobs..."
              kubectl delete jobs -n {{ .Release.Namespace }} \
                -l app.kubernetes.io/managed-by=k8sinterpreter \
                --ignore-not-found=true
              echo "Cleanup complete."
{{- end }}
