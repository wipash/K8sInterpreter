{{- if .Values.execution.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "k8sinterpreter.executorServiceAccountName" . }}
  namespace: {{ include "k8sinterpreter.executionNamespace" . }}
  labels:
    {{- include "k8sinterpreter.labels" . | nindent 4 }}
    app.kubernetes.io/component: executor
  {{- with .Values.execution.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
